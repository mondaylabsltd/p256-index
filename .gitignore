# Rust build output (workspace target lives at the repository root)
/target
# p256-index-cf is excluded from the workspace and builds into its own tree
p256-index-cf/target
p256-index-cf/build
p256-index-cf/.dev.vars
p256-index-cf/.wrangler

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# backups
backups/

# Foundry build artifacts (contracts/)
contracts/out
contracts/cache
contracts/broadcast

.VSCodeCounter